package com.curbme.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Backgrounds ──────────────────────────────────────────────────────────────
val BgDeep      = Color(0xFF080E1A)            // Main app deep dark background
val BgCard      = Color(0xFF111827)            // Card & container surface
val BgElevated  = Color(0xFF1E293B)            // Elevated surface / dialog background
val BgOverlay   = Color(0xFF040814)            // Sub-surface / overlay background

// ── Accents ──────────────────────────────────────────────────────────────────
val AccentBlue   = Color(0xFF3B82F6)           // Primary action blue
val AccentCyan   = Color(0xFF06B6D4)           // Cyan accent
val AccentSky    = Color(0xFF38BDF8)           // Light blue / Shizuku accent
val AccentViolet = Color(0xFF8B5CF6)           // Violet accent
val AccentPink   = Color(0xFFEC4899)           // Pink accent
val AccentAmber  = Color(0xFFF59E0B)           // Warning / Gold accent
val AccentRed    = Color(0xFFEF4444)           // Critical / Error red accent
val AccentGreen  = Color(0xFF22C55E)           // Success / Green accent

// ── Text & Content ───────────────────────────────────────────────────────────
val TextPrimary   = Color(0xFFF1F5F9)          // High emphasis text
val TextSecondary = Color(0xFF64748B)          // Medium emphasis text
val TextSubtle    = Color(0xFF94A3B8)          // Helper / subtle text
val TextMuted     = Color(0xFF334155)          // Disabled / muted text

// ── Dividers & Glass ─────────────────────────────────────────────────────────
val DividerColor  = Color(0xFF1E293B)          // Card & list dividers
val GlassBg       = Color(0x12FFFFFF)          // White 7% for glassmorphism
val GlassBorder   = Color(0x28FFFFFF)          // White 16% for glass border
